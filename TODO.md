# TODO

## Infrastructure
- [ ] Create a stable HTTPS URL for the Alice webhook (currently using a temporary trycloudflare.com URL that changes on server reboot). Options: named Cloudflare Tunnel (requires a domain) or ngrok with a free account.

